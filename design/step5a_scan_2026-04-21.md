# Step 5A 준비 스캔 — V4 확장 대상 정리 (2026-04-21)

> **리포트 전용**. 실제 수정 없음. 대표님 결정 후 Step 5A 착수.

## 현재 상태 (2026-04-21 기준)

### V4 적용된 곳 (2 호출)
| 파일 | 라인 | 용도 |
|---|---|---|
| `js/52_game_tavern.js` | 68 | genHeroRecruitCards |
| `js/52_game_tavern.js` | 125 | genTavernCards |

### V2 (mkCardEl) 잔존 (12 호출, 7 파일)

| 파일 | 라인 | 화면/용도 | Step 5 순위 |
|---|---|---|---|
| `js/53_game_deck.js` | 54 | **Codex 그리드** (혈통/수집) | **Step 5A — 최우선** |
| `js/53_game_deck.js` | 121 | **Deck view** 정렬된 내 덱 | **Step 5A — 최우선** |
| `js/54_game_castle.js` | 40 | Castle Upgrade 대상 카드 | Step 5B |
| `js/54_game_castle.js` | 95 | Castle Quest 대상 카드 | Step 5B |
| `js/55_game_battle.js` | 76 | Matching 내 영웅 카드 (200×250) | Step 5B |
| `js/55_game_battle.js` | 104 | Matching 봇 영웅 카드 | Step 5B |
| `js/55_game_battle.js` | 114 | Matching 재표시 | Step 5B |
| `js/55_game_battle.js` | 192 | **Formation 출전 동료 선택** | **Step 5A — 2번째** |
| `js/59_game_battle_round.js` | 163 | 라운드 보상(유닛 선택) | Step 5C |
| `js/60_turnbattle.js` | 64, 103 | **레거시** turnbattle (v1) | 건드리지 않음 |
| `js/99_bootstrap.js` | 211 | 온보딩 샘플 카드 | Step 5D (안해도 됨) |

## Step 5A 구체적 교체 플랜

### 5A-1. Deckview (`js/53_game_deck.js` 2 호출)
```js
// 54번줄:  const el=mkCardEl(u);  (Codex 수집)
// 121번줄: const el=mkCardEl(c);  (Deck 정렬)
// → 둘 다 mkCardElV4(u) / mkCardElV4(c)
```
**공수**: 2 줄 교체 ~5분
**리스크**: `.dv-grid` 등 레이아웃이 V2 크기(260×390) 기준. V4 기본 260×455 (길이 +17%) → 세로 스크롤 영향. CSS 높이 조정 필요할 수도.
**검증**: Playwright Codex/Deck 스크린샷 전후 비교

### 5A-2. Formation (`js/55_game_battle.js:192`)
```js
// 전투 편성 화면 "출전 동료 선택" 그리드
```
**공수**: 1 줄 교체 + 그리드 scope 조정
**리스크**: Formation 화면 현재 버그 있음 (ui-inspector 보고 — 자동 배치 안 됨). V4 교체 전에 버그 수정 먼저 권장.
**검증**: Playwright Formation 스크린샷

## Step 5B ~ 5D (후속)

### Step 5B: Castle + Matching (4 호출)
- Castle upgrade/quest 카드 — 업그레이드 효과 미리보기 UI, V4 로 갈아도 무해
- Matching 영웅 초상화 — 200×250 사이즈라 V4 크기 재조정 필요

### Step 5C: Round Reward (1 호출)
- `js/59_game_battle_round.js:163` — 라운드 끝 보상 카드 선택 3지선다
- Battle V2 화면 안이라 작게 보여야 함

### Step 5D: Onboarding (1 호출, 낮은 우선)
- `js/99_bootstrap.js:211` — 튜토리얼 샘플 카드
- 튜토리얼 트리거 시에만 보이므로 나중에

## Legacy: turnbattle.js (2 호출 — 건드리지 않음)
`60_turnbattle.js` 는 v1 전투로 `60_turnbattle_v2.js` (PHASE 3 시네마틱) 이 이미 대체. 표시 전략은 FEATURE.CINEMATIC_BATTLE 플래그. V4 확장 대상 아님.

## 제약 사항

### mkCardElV4 에 없는 기능 (Step 5 전 추가 필요)
code-review (`code_review_step4_2026-04-20.md`) 에서 지적된 B2:
- `setHP(n)` / `setNRG(n)` / `setShield(n)` — 전투 중 상태 업데이트 메서드 부재
- `setStatus(status, turns)` — burn/poison 배지 갱신
- `setSelected(bool)` — 선택 상태 외곽선
- mkCardEl (V2) 의 `CardComponent` 가 제공하는 setter API 를 V4 에도 이식 필요

**Step 5C (전투 이식) 전제**: 위 setter 6~8개 구현. 공수 ~1.5시간.

**Step 5A (Deckview + Formation) 는 정적 렌더라 setter 불필요** → 바로 진행 가능.

## 추천 실행 순서 (대표님 결정 후)

1. **🔴 Step 5A-1 Deckview** (~30분, 회귀 + 시각 검증 포함)
2. **🔴 Step 5A-2 Formation** (~1시간, 자동 배치 버그 수정 포함)
3. **🟡 B2 setter API 이식** (~1.5시간) — Step 5C/5B 전제
4. **🟡 Step 5B Castle + Matching** (~1시간)
5. **🟢 Step 5C Battle round reward** (~1시간)
6. **🟢 Step 5D Onboarding** (~30분)

**총 합**: ~5.5시간

## 다음 세션 첫 지시

대표님이 "5A 진행" 하시면:
1. Deckview 2 호출 교체
2. Formation 그리드 V4 스코프 추가
3. Playwright 스크린샷
4. 회귀 9/9
5. 커밋

실제 수정은 모두 **대표님 승인 후**.
