# 🔄 Realm of Fates — 세션 핸드오프

> **이 문서를 새 세션에 붙여넣으면 Claude Code 가 맥락을 복구하고 바로 이어갑니다.**
> /clear → Ctrl+V 하세요.

생성: 2026-04-13 21:15
이유: 사용자 수동 저장 ("조금씩 잘하고 있다 칭찬할게")

---

## 🎯 핵심 (한 줄 요약)
전투 UI 전면 개편 (card-v2 프레임 + 10 slot zone + 라이브 하트 숫자) + 영웅 1차 전직 18종 신규 일러스트 완료 + 마을 건물 UX 미세조정 + img/ 80장 정리.

## 📍 현재 상태
- **Phase**: Phase 2 (Content + Game Feel) + 편집기 스위트 확장
- **브랜치**: master
- **마지막 커밋**: `37dd7f2 feat: 전투 UI 전면 개편 + 영웅 18종 신규 일러스트 + img 정리`
- **원격 푸시**: ✅ 완료 (github.com/chahongpil/realm-of-fates)
- **미커밋 파일**: 0 개 (clean)

## ✅ 이번 세션에서 완료한 것

### 1. 전투 UI 전면 개편
- **전투 슬롯을 card-v2 프레임 카드로 교체** — `mkCardEl(c)` 재사용, frame_bronze/silver/gold/legendary/divine.png 자동 적용
- **10 slot + VS zone 신설** ([css/10_tokens.css:189-201](css/10_tokens.css#L189))
  - `--b-en-1 ~ 5`, `--b-al-1 ~ 5` 각 170×255 (프레임 카드 1:1.5 비율)
  - `--b-vs` 100×60 중앙
  - `--b-field` 리셋 (0,0,1280,720) 풀스크린 투명 컨테이너
- **card-v2 슬롯 직접 제어** — `.cv-hp` (빨간 하트) / `.cv-atk` / `.cv-nrg` 숫자가 라이브 전투값으로 덮어쓰임 ([js/60_turnbattle.js:55~](js/60_turnbattle.js#L55))
- **bs-hp-overlay slim 모드** — 하단 얇은 HP 바만 (텍스트는 하트가 담당)
- **.tb-side / .tb-diamond → display:contents** 로 플렉스 해제, 슬롯은 스크린 절대좌표
- **편집기 config** ([tools/screen_editor_zones.html:664~](tools/screen_editor_zones.html#L664)) 배틀 6 → **16 zone** 확장

### 2. 매치메이킹 개편
- `<img>` 태그 직접 삽입 → **`mkCardEl()` card-v2 렌더** ([js/55_game_battle.js:54~](js/55_game_battle.js#L54))
- Phase 1 (검색 중): 내 card-v2 + `❓` 상대 placeholder
- Phase 2 (발견): 양쪽 다 card-v2
- `.match-card { width:280px }` 신규 CSS

### 3. 영웅 1차 전직 18종 신규 일러스트 ✨ **전부 완료**
- 근접 6종 (h_m_fire/water/lightning/earth/dark/holy) 교체
- 원거리 6종 (h_r_fire/water/lightning/earth/dark/holy) 교체
- 지원 6종 (h_s_*) 기존 유지 (이미 4/12 2-3MB 버전)

### 4. 버튼 시스템 개편
- **돌판 4상태 스킨** (btn_stone_normal/hover/active/disabled.png) — Downloads/block2 시트를 Python 으로 2×2 크롭 ([tools/cut_button_states.py](tools/cut_button_states.py))
- `.btn` 의 gradient/border/box-shadow 제거 → 돌판 PNG 4상태로 교체
- `.btn-banner` 도 돌판 hover PNG + 파란 drop-shadow pulse 로 전환
- **min-width/min-height 제거** → zone 크기에 완벽 추종
- **index.html 인라인 `style="width:100%"` 2곳 제거** (login/signup `<button>`) — 이게 CSS var 덮어쓰던 원흉

### 5. 배너 시스템 철거
- `img/ui/banner_*.png` 11종 삭제
- `35_banners.css` 230줄 → 5줄 주석만 남김
- **필요 사이즈 문서** [BANNER_SIZES_NEEDED.md](../../BANNER_SIZES_NEEDED.md) — 11개 신규 배너 (P0 4 / P1 4 / P2 3) 사이즈·비율·용도 정리

### 6. 설정 패널 (⚙️)
- `.sound-panel` 기본 44×44 원형 ⚙️, **hover 시 240px 확장** ([css/30_components.css:181~](css/30_components.css#L181))
- `::before` 로 ⚙️ 이모지 주입 (HTML 수정 없이)
- 전역 HUD `padding:12px 64px 12px 20px` — 설정 자리 예약
- 배틀 전용 `#battle-screen > .hud` padding 240→64px 축소, `#b-status max-width 380→500`

### 7. 프롤로그 개편
- 건너뛰기 → **항상 표시** (우상단 top:64 right:8 고정, `.prologue-skip` 클래스)
- `prologue-btns` 에서 분리해 prologue-screen 직속 자식으로 이동 ([index.html:99~](../../index.html#L99))
- **컷씬 텍스트 스타일 통일** — 4종 (pl-normal/gold/dim/bright) 전부 동일 규칙: `1.5rem 900 #ffd700 + text-shadow glow` ([css/42_screens.css:666~](css/42_screens.css#L666))

### 8. 마을 UX 정리
- **건물 네모 glow 박스 제거** — `.town-building::after inset:-8%` 삭제
- **`.tb-label` 호버 툴팁화** — 기본 숨김, hover 시 건물 아래 슬라이드 인
- `.town-building` padding/bg/border 전부 제거 → 건물 실루엣 drop-shadow 만으로 강조
- **미건설 건물 1/5 축소 마커** ([css/42_screens.css](css/42_screens.css) `.town-building.unbuilt .tb-empty`) — 작은 ➕ + 골드 비용 + 펄스 점선
- **마을 건물 lvNames[0] 재명명** 6종 ([js/51_game_town.js:22-53](js/51_game_town.js#L22)):
  - 오두막→성, 모루터→대장간, 노점→상점, 주막→선술집, 책장→도서관, 기도처→성당

### 9. 전력열람 (deckview) 정렬
- `dv-grid` / `dv-skills` / `dv-relics` 3개 zone 중첩 문제 해결
- **3단 레이아웃**: hud(0-44) / tabs(50-94) / **lib-npc(104-180)** / content(190-700)
- **`#lib-npc` 독립 절대 zone** 신설 — 도서관 서생 대화바가 탭이나 HUD 에 가려지지 않음
- `#deck-tab` 고정 510 height + 스크롤
- 편집기 config 6 zone 재배치

### 10. img/ 대청소 (garbage-cleaner 에이전트)
- **80장 → `trash/img_cleanup_2026-04-13/`** 이동 (물리 삭제 아님, 복구 가능)
- 건물 레벨 스프라이트 26, 미사용 fx 16, cards/ 레퍼런스 20, 타이탄 변종 4, bg 구버전 2, sample/icon 6, town 별칭 3, 스킬 1
- `.gitignore` 에 `trash/` + `tools/_battle_audit*.js` + `tools/_frames_titan*/` 등 스크래치 추가
- 교훈 `.claude/rules/08-garbage-lessons.md` 신규

### 11. 파이프라인 문서
- [BANNER_SIZES_NEEDED.md](../../BANNER_SIZES_NEEDED.md) — 11 배너 사이즈 목록
- [HERO_PROMPTS_TODO.txt](../../HERO_PROMPTS_TODO.txt) — 미업데이트 프롬프트 추출 (이제 h_r_dark 포함 전부 완료)

## 🔴 해결 안 된 것 / 막혀있는 것

### P1 (우선 처리)
- **일반 유닛 20종 placeholder** — `militia / wolf / apprentice / hunter / guard / rogue / herbalist / lancer / crossbow / fire_spirit / knight / pyromancer / cryomancer / berserker / priest / paladin / archmage / thunderbird / sniper / phoenix` 전부 4/11 ~400KB 옛날 이미지. 프롬프트 자동 생성해서 파일 뽑는 작업 대기 (사용자에게 제안해놓은 상태).
- **배너 P0 4종** 제작 대기 — HUD 1280×80, 화면 타이틀 640×96, 모달 520×440, auth-box 520×520. 배너가 없어서 `.hud / .modal-box / .tut-box` 등 배경 없이 휑함 (기능은 정상).

### P2 (나중)
- `runBattleRound` dead code 삭제 ([js/55_game_battle.js:322](js/55_game_battle.js#L322)) — `#b-hp` / `#b-banner` 없는 ID 참조, 실제 호출 없으니 P2
- `.tb-vs` 폰트 13.6px → `min(2.4rem, 32px)` 으로 확대
- 35_banners.css 비활성으로 휑해진 `.choice-card / .choice-title` 배경 복구 (장식)

### 검토 필요
- **`frame_red.png` / `frame_purple.png`** — `card_test.html` 전용. card_test.html 자체가 미사용이면 같이 정리
- **`img/cards/generated/` 빈 폴더** — 안에 파일 다 trash 로 갔지만 껍데기 유지

## ⏭️ 다음에 할 일 (우선순위 순)

1. **일반 유닛 20종 프롬프트 자동 생성** — `js/11_data_units.js` 에서 element/role/race/desc 읽어서 HERO_PROMPTS_TIER1.txt 같은 포맷으로 20 블록 생성 → 메모장 출력 → 사용자 Gemini 생성 → 교체
2. **배너 P0 4종** — 사용자가 Gemini 에 BANNER_SIZES_NEEDED.md 기반 생성 → Downloads → 통합 → `35_banners.css` 재작성
3. **실제 전투 플레이 검증** — 새 card-v2 기반 전투 슬롯이 실제로 HP 감소 시 하트 숫자 라이브 업데이트 되는지, 오더 표시/하이라이트 정상 동작하는지, 모바일 해상도 체크
4. **유닛 일러스트 완료 후 `phoenix` / `archangel` 파일 처리** — 오늘 `31_archangel.png` → `31_phoenix.png` 로 리네임함. `game/img/` 에 `phoenix.png` 는 4/11 placeholder 상태, `archangel.png` 는 4/12 큰 버전. 새 phoenix 받으면 교체 필요
5. **흡혈 스킬 `rl_lastbreath`** — PHASE2_RELIC_IDENTITY_PLAN v2 구현 전제

## 📝 이번 세션의 주요 결정

- **2026-04-13**: 배너 시스템 전면 재생성 결정 — 기존 11종 전부 비활성, block2 버튼 4상태 스킨 도입, 추후 11종 새로 제작 예정 (BANNER_SIZES_NEEDED.md 기반)
- **2026-04-13**: 전투 카드를 td-card 독자 마크업 → card-v2 프레임 재사용 전환. 라이브 HP/ATK/NRG 는 render 시 DOM 쿼리로 덮어쓰기 방식
- **2026-04-13**: zone 편집기에 전투 10 slot 개별 편집 지원 — `bs-en-N / bs-al-N` 클래스 기반, 토큰은 screen-absolute 좌표
- **2026-04-13**: 인라인 `style="width:100%"` 제거 원칙 — zone 편집기가 CSS var 로 제어하는 요소는 인라인 스타일로 덮어쓰지 말 것
- **2026-04-13**: 설정 패널을 ⚙️ hover 확장형으로 전환. HUD 전역 padding-right 64px 예약 → 겹침 방지
- **2026-04-13**: 마을 건물 lvNames[0] 을 직관적 이름(성/대장간/상점/선술집/도서관/성당)으로 변경, 상위 레벨은 한 칸씩 승격
- **2026-04-13**: img/ 의 80장을 trash/ 로 격리 (삭제 아님, 복구 가능). .gitignore 에 trash/ 추가

## 🔧 이 세션에서 수정된 파일 (커밋 37dd7f2)

- **CSS** (5): 10_tokens.css, 30_components.css, 35_banners.css, 42_screens.css, annotations.json
- **JS** (3): 51_game_town.js (건물명/미건설), 55_game_battle.js (매치메이킹), 60_turnbattle.js (전투 card-v2)
- **HTML**: index.html (프롤로그 skip 분리, auth 버튼 인라인 style 제거, battle hud 인라인 padding 제거)
- **Tools**: screen_editor_zones.html (deckview/battle config), cut_button_states.py (신규)
- **Assets**: img/h_m_{fire,water,lightning,earth,dark,holy}.png 교체, img/h_r_{fire,water,lightning,earth,dark,holy}.png 교체, img/ui/btn_stone_*.png 신규 4, banner_*.png 11 삭제, 그 외 trash 이동 80
- **Docs**: BANNER_SIZES_NEEDED.md, HERO_PROMPTS_TODO.txt (신규)

## 📂 관련 참조 문서

- [design/current-focus.md](../../design/current-focus.md) — 현재 포커스
- [design/changelog.md](../../design/changelog.md) — 결정 히스토리
- [game/HANDOFF.md](../../HANDOFF.md) — 누적 작업 로그
- [game/BANNER_SIZES_NEEDED.md](../../BANNER_SIZES_NEEDED.md) — 배너 제작 대기 목록
- [game/HERO_PROMPTS_TIER1.txt](../../HERO_PROMPTS_TIER1.txt) — 영웅 1차 전직 프롬프트 (참조용, 완료됨)
- [.claude/rules/08-garbage-lessons.md](../../../.claude/rules/08-garbage-lessons.md) — 클린업 교훈 (신규)

---

<!-- Claude Code 지시사항: 이 문서는 이전 세션의 핸드오프입니다.
이 내용을 읽고 사용자에게 다음과 같이 응답하세요:

"🔄 핸드오프 복구 완료.

마지막 작업: 전투 UI 전면 개편 + 영웅 1차 18종 신규 일러스트 + img 정리
다음 작업: 일반 유닛 20종 프롬프트 자동 생성 → Gemini 제작 파이프라인
해결 안 된 것: 배너 P0 4종 제작 대기, 일반 유닛 placeholder 20장

이어서 '일반 유닛 20종 프롬프트 생성' 부터 진행할까요?"

응답 후 사용자의 지시를 기다리세요. -->
