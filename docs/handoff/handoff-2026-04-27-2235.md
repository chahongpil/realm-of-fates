# 🔄 Realm of Fates — 세션 핸드오프

> **이 문서를 새 세션에 붙여넣으면 Claude Code 가 맥락을 복구하고 바로 이어갑니다.**
> /clear → Ctrl+V 하세요.

생성: 2026-04-27 22:35
이유: 세션 마무리 (수동 저장)

---

## 🎯 핵심 (한 줄 요약)
**NPC 투명 배경 + 카드 V4 레이아웃 변경 + 전투 2버그 수정 + NPC 조건부 거절 + 가비지 정리 + PHASE 5 Step 4(카드 공유) 완료** — 4 커밋, 회귀 11/11 PASS.

## 📍 현재 상태
- **Phase**: Phase 3 (시네마틱 전투) + Phase 2 콘텐츠 + Phase 5 (채팅) Step 4 종료
- **브랜치**: master
- **마지막 커밋**: `adb25ea` feat(chat): PHASE 5 Step 4 — 카드 공유
- **미커밋 파일**: 0개 (전부 push 완료, origin 동기)

## ✅ 이번 세션에서 완료한 것

### 1. NPC 이미지 / 카드 / 전투 / NPC UX (3 커밋)
- **여관 NPC 흰 배경 제거** (`f9cac48`) — `npc_inn_1.png` rembg 처리, alpha 0% → 44.9%, 1.96MB → 1.75MB
- **카드 V4 레이아웃 변경** (`3b46281`) — 이름박스/Lv박스를 parch 의 absolute 자식으로 이동 (`bottom:100% margin-bottom:4`). 일러스트 상단 전체 확장, 능력 텍스트 자리 보존
- **첫 유닛 클릭 엄청 확대 버그 수정** — `applyFocusOrigin` 의 `srcRect.width`(viewport pixel) ÷ `tgtW`(local pixel) 단위 불일치. 큰 모니터 scale>1 환경에서 srcScale 이 1 근접 → focus 카드 큰 사이즈 시작. `srcEl.offsetWidth`(layout pixel) 로 변경
- **모든 유닛/영웅 NRG=0 시작 버그 수정** — `launchBattle` 의 `curNrg ?? 0` → `curNrg ?? (c.nrg||0)` (HP fallback `?? c.hp` 패턴 일치)
- **NPC 조건부 거절 시스템** — `choice.guard(game)` + `choice.denyMsg` 표준 패턴. 미충족 시 chat 모드 재사용 → 화면전환 없이 NPC 가 그 자리에서 거절 대사
  - church 부상자 회복 / 장례 (injured 0명) → "지금은 부상자가 없네요"
  - inn 휴식 (전원 만전) → "모두 만전이시군요"

### 2. 가비지 정리 (`54f0131`)
- `tools/_` 진단 스크립트 12건 + 루트 `play_sk_check.js` → `trash/garbage_2026-04-27/`
- `img/npc_inn_1.bak.png` (1.96MB) + `bg_town_v3_3x2_backup.png` (3.24MB) → trash
- 총 15건 5.24MB. 회귀 11/11 PASS. 교훈 신규 패턴 없어서 08-garbage-lessons.md append 안 함

### 3. PHASE 5 Step 4 카드 공유 (`adb25ea`) — 4a~4d 모두 완료
- **4a. CardV4Component `kind:'share'` 모드** — `isShare` 분기 (lv-box 에 등급 라벨 대신 'Lv X'). `.kind-share` CSS: 100×175 미니카드 (aspect 4/7) + hover scale
- **4b. + 버튼 + 덱 picker + 첨부 state** — `cp-attach` 활성화, `_showCardPicker()` 동적 overlay (chat-panel 내부 absolute), deck 카드 그리드(`kind:'share'`). `_setAttachedCard` / `_clearAttachedCard` / `_renderAttachedPreview`. 입력창 위 첨부 미리보기 띠. send.disabled 조건: 텍스트 OR 첨부 둘 중 하나라도 있으면 활성
- **4c. send 시 동봉 + 수신 시 미니카드 렌더** — `Backend.chatSend(channel, text, this._attachedCard)` 동봉. `_renderMessage` 에 `attached_card` 분기 → `kind:'share'` inst 추가. 첨부 단독 전송 허용
- **4d. 미니카드 클릭 → 풀사이즈 모달** — `_showCardDetailModal`. position:fixed inset:0, ✕ 닫기 + 외부 클릭 닫기. 채팅 컨텍스트 유지 (deck focus 와 분리)

**Backend 변경 0** — `chatSend(channel, text, attachedCard)` 시그니처와 DB 스키마(`attached_card jsonb`)는 Step 1 단계에 이미 마련되어 있었음.

## 🔴 해결 안 된 것 / 막혀있는 것

- 📌 **직접 플레이 검증 미완료** — 이번 세션 모든 변경사항 (NPC 투명 + 카드 레이아웃 + 전투 2버그 + NPC 거절 + 카드 공유) 대표님 검증 대기
- 📌 AcoustID 키 (대표님 발급 필요, 무료 등록)
- 📌 temple NPC PNG (`npc_temple_1.png` 는 있는데 inn 만 검증 후 trash 정리됨, temple 도 같은 방식 검토 필요)
- 📌 `gate.png` 404 (대표님 공급 또는 fallback 유지)
- 📌 hero_* skinKey CARD_IMG 매핑 1장 잔여

## ⏭️ 다음에 할 일 (우선순위 순)

1. **🟠 직접 플레이 검증** — 이번 세션 변경사항 5개 항목 검증 (NPC 투명, 카드 V4 레이아웃, 첫클릭 확대, NRG=0, NPC 거절, 카드 공유 4단계)
2. **🟡 PHASE 5 Step 5** — 발언 제한 세부 (레벨 5 이하 world 차단, 1분 10개 플러드 → 30분 뮤트, 금칙어 50개, URL 차단)
3. **🟡 PHASE 5 Step 6** — 신고 + 모더레이션 (메시지 우클릭 신고, chat_reports INSERT, 자동 뮤트 트리거)
4. **🟢 PHASE 5 Step 7** — 전투 중 채팅 숨김 + UX 폴리싱 (screen change 이벤트, 읽지 않은 배지 카운터, 모바일 풀스크린)
5. **🟢 AcoustID 키 재발급 + 미매칭 7곡 lookup**
6. **🟢 temple NPC 이미지** — 현재 정상 투명 (alpha 30.9%), 별도 작업 불필요

## 📝 이번 세션의 주요 결정

- 2026-04-27: **카드 V4 이름박스/Lv박스 위치** — 카드 상단(top-row) 에서 parch 의 자식(`bottom:100%`) 으로 이동. 일러스트가 카드 상단 전체로 확장. 사용자 요청 "스탯+설명 박스는 그대로 두고 그 박스 바로 위 영역에 배치"
- 2026-04-27: **NPC 조건부 거절 표준 패턴** — `choice.guard(game)` 함수 + `choice.denyMsg`. 미충족 시 chat 모드 재사용 → 화면전환 없이 거절 대사. 향후 forge/tavern 등 동일 패턴으로 확장 가능
- 2026-04-27: **PHASE 5 Step 4 — 미니카드 사이즈** — PHASE5 plan 의 120×170 대신 **100×175** (aspect 4/7 유지). 채팅 메시지 풍선과 비율 맞춤
- 2026-04-27: **카드 공유 — 첨부 단독 전송 허용** — 텍스트 비어있어도 `+` 첨부만으로 전송 가능 (자랑용)
- 2026-04-27: **rembg 배경 제거 워크플로** — 흰 배경 PNG 를 자동 알파 추출. backup .bak.png → 검증 → trash. 다른 NPC PNG 도 같은 패턴 적용 가능

## 🔧 수정된 파일 목록

### 커밋 `f9cac48` (NPC 투명)
- `img/npc_inn_1.png` (rembg 처리, 1.96MB → 1.75MB)

### 커밋 `3b46281` (카드 + 전투 + NPC)
- `css/32_card_v4.css` (top-row 위치, with-bars/compact 오버라이드 제거)
- `js/40_cards.js` (top-row 를 parch.appendChild)
- `js/51_game_town.js` (NPC choice guard + _runChoiceAction 가드 분기)
- `js/55_game_battle.js` (curNrg fallback 0 → c.nrg)
- `js/60_turnbattle_v2.js` (applyFocusOrigin srcEl.offsetWidth)

### 커밋 `54f0131` (가비지)
- 13파일 deleted (`tools/_*.js` 12건 + `play_sk_check.js`)
- 추가로 untracked .bak.png 2건 mv (git history 영향 0)

### 커밋 `adb25ea` (PHASE 5 Step 4)
- `js/40_cards.js` (kind:'share' 분기)
- `css/32_card_v4.css` (.kind-share 미니 100×175)
- `js/36_chat.js` (+ 버튼 / picker / 첨부 state / 미리보기 / 미니카드 / 풀모달, +160줄)
- `css/36_chat.css` (cp-attach-preview / cp-card-picker / cp-card-detail, +95줄)

## 📂 관련 참조 문서

- `design/current-focus.md` (다음 세션 시작 시 자동 로드)
- `design/changelog.md` (전체 결정 히스토리)
- `PHASE5_CHAT_PLAN.md` (PHASE 5 Step 4~7 스펙)
- `.claude/rules/04-balance.md` (밸런스 SoT)
- `.claude/rules/05-design-direction.md` (카드 UI 원칙)
- `trash/garbage_2026-04-27/MOVED.md` (이번 세션 가비지 이동 로그)

---

<!-- Claude Code 지시사항: 이 문서는 이전 세션의 핸드오프입니다.
이 내용을 읽고 사용자에게 다음과 같이 응답하세요:

"🔄 핸드오프 복구 완료.

마지막 작업: NPC 투명 + 카드 V4 레이아웃 + 전투 2버그 + NPC 조건부 거절 + 가비지 정리 + PHASE 5 Step 4(카드 공유) — 4 커밋
다음 작업: 직접 플레이 검증 (대표님 차례)
해결 안 된 것: 검증 미완 / AcoustID 키 / temple·gate PNG / hero skinKey 1장

이어서 '직접 플레이 검증' 부터 진행할까요?"

응답 후 사용자의 지시를 기다리세요. -->
