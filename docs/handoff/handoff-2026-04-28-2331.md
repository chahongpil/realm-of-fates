# 🔄 Realm of Fates — 세션 핸드오프

> **이 문서를 새 세션에 붙여넣으면 Claude Code 가 맥락을 복구하고 바로 이어갑니다.**
> /clear → Ctrl+V 하세요.

생성: 2026-04-28 23:31
이유: 세션 마무리 (수동 저장)

---

## 🎯 핵심 (한 줄 요약)
**Supabase PAT 누출 사후 정리 + secret-scan 가드 + AcoustID fingerprint 보존 + PHASE 5 Step 5(발언 제한 4 sub-step) 완료** — 6 커밋, 회귀 12/12 PASS.

## 📍 현재 상태
- **Phase**: Phase 3 (시네마틱 전투) + Phase 2 콘텐츠 + **Phase 5 (채팅) Step 5 종료**
- **브랜치**: master
- **마지막 커밋**: `367e62f` docs: PHASE 5 Step 5 종료 — current-focus + changelog 갱신
- **미커밋 파일**: 0개 (전부 push 완료, origin 동기)

## ✅ 이번 세션에서 완료한 것

### 1. 🚨 Supabase PAT 누출 사후 정리 (커밋 `1689186`)
- **누출 경위**: 2026-04-22 핸드오프 `handoff-2026-04-22-2303.md` line 59 에 PAT 평문(`sbp_568b…40hex`) 박힌 채로 commit `928e723` 으로 public repo (`chahongpil/realm-of-fates`) push. 6일 후 GitHub secret scanning → Supabase 자동 revoke + 메일 통보 (2026-04-28).
- **누출 범위 전수 조사**: PAT 1개만, 다른 시크릿(JWT/sk-ant/ghp_/AKIA 등) 노출 없음.
- **Sanitize**: 누출 라인 `[REDACTED — revoked 2026-04-28]` 로 교체. 보안 섹션을 "사후 정리" 형식으로 재작성.
- **재발 방지 가드**: `c:/work/.claude/hooks/post_edit_secrets.js` 신규 (11 패턴: sbp_/JWT/sk-ant/ghp_/AKIA/xoxb/Slack/Google API key/private key block 등). settings.json `PostToolUse → Edit|Write` 등록. 자가 테스트 통과.
- **교훈** `.claude/rules/08-garbage-lessons.md` 5조 추가:
  - 시크릿은 prefix 4자만 문서에 적기 (전체 토큰 금지)
  - 핸드오프 = public repo 라고 가정
  - "revoke 권고 작성 금지 — 즉시 revoke"
  - 암묵 상식은 가드 없으면 결국 한 번은 깨진다
  - public repo 인지 `git remote -v` 로 확인 루틴
- **git history**: A1 (그대로 두고 sanitize 커밋만 push) 채택. 토큰은 무효화 상태라 history 잔존 위험 0.

### 2. AcoustID 7곡 fingerprint 보존 (커밋 `92251f1`)
- `c:/work/tools/fpcalc/fpcalc.exe` 1.5.1 (chromaprint) 설치 완료
- 미매칭 7곡(town1·town2·battle1~5) 모두 정상 fingerprint 추출
- **lookup 보류**: 사용자 제공 키 2개(`Wmr0V4a7M7`, `qMiheLTzso`) 둘 다 AcoustID `invalid API key` 거부. 둘 다 **User API Key** (https://acoustid.org/api-key) 형식, lookup 에 필요한 **Application API Key** 가 아님. https://acoustid.org/new-application 등록 시도 → "Missing email address" 막힘 (계정 프로필에 이메일 누락, MusicBrainz/OpenID SSO 자동 import 안 됨).
- **재시도 절차**: 프로필에 이메일 추가 + 인증 → /new-application 등록 → `ACOUSTID_KEY=<application_key> node c:/work/tools/fpcalc/lookup.js`. 스크립트와 fingerprint 모두 보존됨.

### 3. PHASE 5 Step 5 — 발언 제한 세부 (커밋 `b956616` `e96dafe` `e2bf9c3`)
**5a. Lv5 미만 world 채널 차단** (`b956616`)
- `js/36_chat.js` `MIN_LEVEL = {world:5, league:1, guild:1}` + `_getMyLevel/_minLevelFor/_applyChannelLevelGate` 헬퍼.
- `_sendMessage` 진입부 + 채널 진입(`_switchKind`/`_loadAndSubscribe`) 모두 검증. 미달 시 input 잠금 + "Lv5 부터 발언 가능" 안내.
- `supabase/migrations/004_s5_limits.sql` 신규 — RLS WITH CHECK `channel<>'ch_world' OR user_level>=5`.

**5b. 도배 사전 경고 + 자동 뮤트** (`e96dafe`)
- 클라 sliding window 60초. 8개 도달 시 ⚠️ 사전 경고, 10개 도달 시 🔇 자동 뮤트 안내.
- 서버 trigger `trg_auto_mute_on_flood` (004 migration 동봉) — INSERT 후 60초 ≥10 시 30분 mute upsert. 신고 누적('reports') 사유 보존(우선).

**5c/5d. 금칙어 마스킹 + URL 차단** (`e2bf9c3`)
- 신규 `js/38_chat_filters.js` (89줄): 49개 금칙어(욕설/혐오/광고매크로) 어근 매칭 → 첫 글자 보존 + 별표. URL regex(http(s)//, www., 주요 TLD .com .net .kr .io .co .me .gg .app .tv 등) → 메시지 거부.
- `index.html` script 등록. `_sendMessage` 에서 `RoF.ChatFilters.censor()` 호출 + banner 안내.
- `tools/test_run.js` 12번째 시나리오 추가 — 6 케이스 (정상/욕설/URL 다양) 검증.

### 4. 문서 정리 (커밋 `367e62f`)
- `design/current-focus.md` 갱신 (5건 커밋, 다음 우선순위에 004 migration 수동 적용 항목 추가)
- `design/changelog.md` 5a~5d 단계별 변경/이유/영향 영구 기록

## 🔴 해결 안 된 것 / 막혀있는 것

- 📌 **004_s5_limits.sql Supabase 대시보드 적용 필요** — PAT revoke 상태라 자동 push 불가. 새 PAT 발급 받거나 Supabase SQL 에디터로 수동 실행 필요. 이게 안 되면 5a RLS·5b flood trigger 미적용 (클라 가드만 작동, 서버 가드 0).
- 📌 **AcoustID Application API Key** — User Key 가 아닌 Application Key 가 필요. https://acoustid.org/new-application 등록 시 프로필에 이메일 먼저 추가해야 함 (MusicBrainz/OpenID SSO 가 자동 import 안 함).
- 📌 **직접 플레이 검증 미완료** — 2026-04-27 변경 6건 + 2026-04-28 PHASE 5 Step 5 (Lv5 차단·도배·금칙어·URL) 대표님 검증 대기.
- 📌 `gate.png` 404 (대표님 공급 또는 fallback 유지)
- 📌 `hero_*` skinKey CARD_IMG 매핑 1장 잔여
- 📌 **dead-code 감사 권장** — `60_turnbattle.js` (445줄, 구버전) vs `60_turnbattle_v2.js` (1,614줄, 정본) 공존. v2 가 100% 인계받았는지 확인 후 폐기 가능. `55_game_battle.js` (757줄) 도 v2 이행 후 unused 함수 잔존 의심.

## ⏭️ 다음에 할 일 (우선순위 순)

1. **🟠 직접 플레이 검증** — 2026-04-27 + 2026-04-28 변경 동작 확인 (NPC 투명·카드 레이아웃·전투 2버그·NPC 거절·카드 공유·Lv5 차단·도배·금칙어·URL)
2. **🟡 004_s5_limits.sql 대시보드 적용** — 새 PAT 발급 + 자동 push, 또는 SQL 에디터 수동 실행
3. **🟡 PHASE 5 Step 6** — 신고 + 모더레이션 (003 에 trigger 이미 있음, UI 만 작업: 메시지 우클릭 신고 버튼 + chat_reports INSERT)
4. **🟢 PHASE 5 Step 7** — 전투 중 채팅 숨김 + 읽지 않은 배지 + 모바일 풀스크린
5. **🟢 dead-code 감사 1회** — `garbage-cleaner` 에이전트로 `60_turnbattle.js` (구) / `55_game_battle.js` legacy / 기타 unused 함수 감사. 권장 -500~1000줄 다이어트
6. **🟢 AcoustID Application Key** + 7곡 lookup (1분 작업)
7. **🟢 hero_*  skinKey CARD_IMG 매핑 1장**

## 📝 이번 세션의 주요 결정

- 2026-04-28: **PAT 누출 history 처리 = A1 채택** — 토큰은 이미 무효화라 sanitize 커밋만 push. force-push history 재작성은 비용 대비 효과 0 (이미 외부 노출 시점 + 협업자 충격).
- 2026-04-28: **secret-scan 가드는 처음부터 만들었어야 했다** — 암묵 상식 "PAT 평문 금지"도 가드 없으면 한 번은 깨진다. 이제부터 모든 Edit/Write 자동 차단.
- 2026-04-28: **AcoustID Application Key vs User Key 헷갈림** — User Key (/api-key 페이지, submit 용) ≠ Application Key (/new-application 등록 후, lookup 용). 8자리/10자리 형식 비슷해서 혼동 쉬움. SOURCES.md 에 명시.
- 2026-04-28: **PHASE 5 Step 5 클라/서버 분담** — 5a/5b 는 서버 RLS·trigger + 클라 사전경고 양쪽. 5c/5d 는 클라 단만(서버 jsonb regex 부담). 회피 변형은 Step 6 신고 누적으로 흡수.
- 2026-04-28: **금칙어 50 → 49로 끊음** — false positive 우려 단어 1개 제외. "감탄/관용 표현 보호 우선" 원칙.
- 2026-04-28: **20K 줄 진단** — 인디 카드게임 5K~30K 범위 안. 단 `60_turnbattle.js` (구) vs `_v2.js` 공존이 부채. 다음에 dead-code 감사 권장.

## 🔧 수정된 파일 목록

### 커밋 `1689186` (PAT sanitize + 가드)
- `game/docs/handoff/handoff-2026-04-22-2303.md` (line 59 [REDACTED] 교체)
- `game/design/changelog.md` (2026-04-28 보안 항목 영구 기록)
- _별도 운영 트리_: `c:/work/.claude/hooks/post_edit_secrets.js` (신규), `c:/work/.claude/settings.json` (등록), `c:/work/.claude/rules/08-garbage-lessons.md` (5조 교훈)

### 커밋 `92251f1` (current-focus + AcoustID 보존)
- `game/design/current-focus.md` (PAT 정리 + AcoustID 보류 + Step 5 진입 표기)
- `game/snd/SOURCES.md` (7곡 fingerprint duration 표 + Application Key 막힘 원인 + 재시도 절차)

### 커밋 `b956616` (Step 5a)
- `game/js/36_chat.js` (+43줄: MIN_LEVEL 상수 + 헬퍼 3 + send 검증 + gate)
- `game/supabase/migrations/004_s5_limits.sql` (신규 67줄: 5a RLS + 5b flood trigger 동봉)

### 커밋 `e96dafe` (Step 5b 클라)
- `game/js/36_chat.js` (+16줄: _sentTimestamps 배열 + sliding window + warn/limit banner)

### 커밋 `e2bf9c3` (Step 5c/5d)
- `game/js/38_chat_filters.js` (신규 89줄: PROFANITY 49 + URL_RE + censor/maskProfanity/containsURL)
- `game/index.html` (+1줄: script 등록)
- `game/js/36_chat.js` (+15줄: censor wiring + cleanText 전송)
- `game/tools/test_run.js` (+27줄: chat-filters 테스트 시나리오)

### 커밋 `367e62f` (문서 정리)
- `game/design/current-focus.md` (Step 5 종료 표기 + 다음 우선순위 갱신)
- `game/design/changelog.md` (5a~5d 단계별 영구 기록)

## 📂 관련 참조 문서

- `design/current-focus.md` — 다음 세션 시작 시 자동 로드 (최신 상태)
- `design/changelog.md` — 결정 히스토리 (2026-04-28 보안 + Step 5)
- `PHASE5_CHAT_PLAN.md` — Step 5~7 스펙
- `snd/SOURCES.md` — AcoustID fingerprint + 재시도 절차
- `.claude/rules/08-garbage-lessons.md` — secret-handling 5조 교훈 (운영 트리)
- `.claude/hooks/post_edit_secrets.js` — 11 패턴 가드 (운영 트리)
- `supabase/migrations/004_s5_limits.sql` — Step 5 RLS·trigger (대시보드 수동 적용 대기)

## 🎵 도구 자산
- `c:/work/tools/fpcalc/fpcalc.exe` — chromaprint 1.5.1 (Windows)
- `c:/work/tools/fpcalc/lookup.js` — `ACOUSTID_KEY` env 받아 7곡 일괄 lookup

---

<!-- Claude Code 지시사항: 이 문서는 이전 세션의 핸드오프입니다.
이 내용을 읽고 사용자에게 다음과 같이 응답하세요:

"🔄 핸드오프 복구 완료.

마지막 작업: PAT 누출 정리 + secret-scan 가드 + AcoustID fingerprint + PHASE 5 Step 5 4 sub-step 완료 — 6 커밋
다음 작업: 직접 플레이 검증 (대표님 차례) — 2026-04-27 변경 6건 + Step 5 동작 확인
해결 안 된 것: 004_s5_limits.sql 대시보드 적용 / AcoustID Application Key / dead-code 감사 / 직접 검증 미완

이어서 '직접 플레이 검증' 부터 진행할까요? 아니면 PHASE 5 Step 6 (신고 UI) 으로 바로 넘어갈까요?"

응답 후 사용자의 지시를 기다리세요. -->
