# 🔄 Realm of Fates — 세션 핸드오프

> **이 문서를 새 세션에 붙여넣으면 Claude Code 가 맥락을 복구하고 바로 이어갑니다.**
> /clear → Ctrl+V 하세요.

생성: 2026-04-29 01:18
이유: 세션 마무리 (수동 저장 — 영웅 시스템 단일화 + 전투 속도 + 직업 트리 결정)

---

## 🎯 핵심 (한 줄 요약)
**직접 플레이 검증 중 발견 버그 4건 fix + 자율감사 3종 + 영웅 시스템 단일화 + 전투 속도 절대값 상향 + 직업 트리 시스템 결정** — 2 커밋, 회귀 12/12 PASS.

## 📍 현재 상태
- **Phase**: Phase 3 (시네마틱 전투) + Phase 2 콘텐츠 + Phase 5 채팅 Step 5 종료
- **브랜치**: master
- **마지막 커밋**: `9de8017` feat(hero+battle): 영웅 시스템 단일화 + 전투 속도 B+D + 인벤토리 문서
- **이전 커밋**: `dfea28d` fix(town+battle): NPC 화면 진입 + AoE dim 누수 + NRG 이중방어
- **미커밋 파일**: 0개 (origin 미동기화 — push 안 함)

## ✅ 이번 세션에서 완료한 것

### 1. 직접 플레이 발견 버그 fix 4건 (커밋 `dfea28d`)
- **도서관 NPC 액션** — `showDeckTab`/`showCodexTab` 이 화면 진입 없는 탭 전환 메서드라 클릭 무반응. `showCodexView` wrapper 신규.
- **선술집 NPC 액션** — 같은 패턴. (단 영웅 영입은 후속 폐기로 wrapper 도 같이 삭제됨)
- **왕궁 "퀘스트 받기"** — 추가 발견. `showCastleQuestEntry` wrapper.
- **AoE 스킬 dim 누수** — 번개사슬(all_enemies) 시전 후 우리편 모두 검은색 회색 잔류. `is-dimmed` 클래스가 `startCombatExecution` 라운드 종료 + `onCharClick` 두 지점에서 안 풀리던 문제. `clearTargetHighlight()` 추가.
- **NRG 이중방어** — `legacyCardToV2Unit:1419` `?? 0` → `?? c.nrg ?? 10` (HP 패턴 일치).

### 2. 자율학습 3 감사 (changelog 영구 기록, 변경은 별도 처리 대기)
- **balance-auditor**: P0 5건 — `archmage` hp 12, `sniper` hp 10, `lich` hp 20 (gold/legendary attack 최솟값 미달). `pirate`(rarity bronze 오기재 → silver), `dark_shaman`(silver → gold) 정정. divine defense/support 셀 0종.
- **lore-consistency-auditor**: Critical 5건 — `PHASE3_COATING_PLAN.md` / `PHASE1_PLAN.md` / `PHASE2_TOWN_PLAN.md` 에 "가챠/뽑기/뽑기권" 표현 잔존 (monetization v2.1 § 0 위반). `pirate`/`dark_shaman` rarity 오기재 (balance 와 동일).
- **garbage-cleaner**: `60_turnbattle.js` (445줄 구버전) **100% dead 아님** — 2 fallback 진입점 (`55:339`, `62_ghost_pvp:150`). archive 권장 26건 (design 19 + tools 7) — 약 -2K 줄 감량 가능.

### 3. 영웅 시스템 결정 + 단일화 (커밋 `9de8017`)
- **결정**: 영웅 = 유저 카드 1장 고정. 회원가입 시 1명, 추가 영입 불가.
- **선술집 영웅 영입 시스템 완전 폐기** — `_tavTab` / `showTavernHero` / `showTavernHeroEntry` / `genHeroRecruitCards` 4종 삭제. `index.html` 영웅 탭 버튼도 제거.
- 같은 세션 초반에 fix 한 "선술집 영웅 영입 등급 캡 시스템(`createHero` + RARITY_W/LV)" 코드도 함께 폐기 (의미 무효화).

### 4. 전투 속도 B+D (커밋 `9de8017`)
- **B (TIMING 4개 절대값 상향)** `config_battle.js`:
  - `CHAR_FOCUS_IN` 350→500, `FIRE_TRAVEL` 450→700, `HIT_SHAKE` 350→550, `BETWEEN_ACTION` 250→450
  - 1 액션 1.7~2초 → 2.6~3초. CSS 변수도 boot 타임에 자동 반영.
- **D (속도 토글 UI)** `index.html` + `css/41_battle_v2.css` + `js/60_turnbattle_v2.js`:
  - `bv2-hud-top` 좌측에 ×1/×2/×4 버튼 그룹. `data-speed` 속성으로 매직 넘버 제거. `is-active` 동기화.

### 5. 영웅/유닛/스킬 종합 인벤토리 문서
- `design/units_skills_inventory_2026-04-29.txt` 신규 (이펙트 이미지 제작 참고용)
- PART 1: 영웅 시스템 36 조합 / PART 2: 일반 유닛 51종 / PART 3: 스킬 44종 / PART 4: bonusTrigger 분류 / PART 5: 이펙트 우선순위 / PART 6: 애니메이션 워크플로 (Sprite Sheet + AnimateDiff + CSS steps())

### 6. 08-garbage-lessons.md 6번째 교훈
- **HTML ↔ JS 경계 element/참조 동기화** — element 폐기 시 JS getElementById 참조 누락. "선언/참조 동기화 누락" 패턴의 5번째 변형 (이전 4건은 JS 내부, 이번부터 HTML ↔ JS 확장). PostToolUse 회귀 hook 1회 실패 후 즉시 fix + 교훈 증류.

## 🔴 해결 안 된 것 / 막혀있는 것

### 다음 세션 큰 작업 — 직업 트리 시스템 (5단계 의존)
영웅 전직 시스템을 사용자 결정대로 구현하려면 선행 시스템 부재로 단계 분리 필요.

**대표님 결정 사항 (2026-04-29 밤)**:
- 전직 = 원소 + 역할 둘 다 변경 가능
- 비용 = 골드만
- 레벨업 시 freePoints +1 (현재 +5)
- lv 5당 등급 자동 상승 폐기
- 등급 상승 = 별도 자격 (직업 카드 해금 = 영웅 lv + 무기 lv + 스펠 lv 3축)
- 예시: 마검사 = 영웅 lv 20 + 칼 무기 lv 20 + 6원소 중 스펠 lv 20

**선행 시스템 부재**:
- 무기 레벨 시스템 ❌ (equips 슬롯만 있음, lv 개념 없음)
- 스펠 레벨 시스템 ❌ (스펠은 등급만, lv 없음)
- 직업 카드 데이터 ❌
- 직업 해금 판정 로직 ❌
- 왕궁 전직 UI ❌
- 6원소 스펠 분포 비대칭 (fire 4 / water 1 / lightning 2 / earth 1 / holy 3 / dark 3) — 마검사가 "원소 중 스펠 lv 20" 룰 가지면 일부 원소 해금 불가

### 누적 미해결
- 📌 **직접 플레이 검증** — 2026-04-27 변경 6건 + Step 5 + 이번 세션 변경 (속도 UI / 도서관·선술집·왕궁 NPC / 번개사슬 dim / 선술집 단일 탭) 대표님 검증 대기
- 📌 **balance P0 5건 미수정** — archmage/sniper/lich hp 상향 + pirate/dark_shaman rarity 정정 (자율감사 결과, changelog 만 기록)
- 📌 **lore Critical 5건 미수정** — PHASE 1/2/3 기획서 "가챠/뽑기/뽑기권" 표현 제거
- 📌 **PHASE 5 Step 6** — 신고 + 모더레이션 UI (003 trigger 이미 있음 → UI 만)
- 📌 **004_s5_limits.sql Supabase 대시보드 적용** — PAT revoke 상태라 자동 push 불가
- 📌 **AcoustID Application API Key** — User Key 가 아닌 Application Key 발급 필요 (/new-application)
- 📌 **bonusTrigger v2 시스템 이식** — 영웅 36 조합 시그니처 효과 0% 발동 (큰 작업, 별도 phase)
- 📌 **dead-code archive 26건** — design/ 19 + tools/ 7 (`60_turnbattle.js` 100% dead 아님 — fallback 진입점 검증 필요)
- 📌 `gate.png` 404 (대표님 공급 또는 fallback 유지)
- 📌 `hero_*` skinKey CARD_IMG 매핑 1장 잔여

## ⏭️ 다음에 할 일 (우선순위 순)

1. **🟠 Step 1: 직업 트리 기획서 작성** — `design/job_system_plan.md` 신규
   - 직업 N개 리스트 (마검사 / 성기사 / 암살자 / 정령사 등 사용자 결정)
   - 각 직업 해금 조건 3축: 영웅 lv + 무기 lv + 스펠 lv
   - 전직 후 효과 (역할 변경 / 원소 변경 / 스킬셋 변경)
   - 골드 비용 산정
2. **🟡 Step 2: 무기 레벨 시스템** — 데이터 + 강화 메커닉 + 대장간 UI
3. **🟡 Step 3: 스펠 레벨 시스템** — 같은 패턴
4. **🟡 Step 4: 직업 데이터 + 해금 판정 로직**
5. **🟡 Step 5: 왕궁 전직 UI**
6. **🟢 직접 플레이 검증** — 이번 세션 변경 동작 확인
7. **🟢 balance P0 5건 적용** — 데이터 직접 수정
8. **🟢 lore Critical 5건 적용** — PHASE 기획서 가챠 표현 정리

## 📝 이번 세션의 주요 결정

- 2026-04-29: **영웅 = 유저 카드 1장 고정** — 36 조합 매트릭스 중 회원가입 시 1개 선택, 추가 영입 불가. 선술집은 용병 전용.
- 2026-04-29: **단련 룰 변경 예정** — freePoints +5 → +1, lv 5당 등급 자동 상승 폐기, 별도 자격(직업 카드 해금)으로 등급 상승.
- 2026-04-29: **전직 시스템 = 직업 카드 해금 + 선택** — 영웅 lv + 무기 lv + 스펠 lv 3축 충족 → 카드 해금 → 골드로 전직. 비용 = 골드만, 신의 축복 X.
- 2026-04-29: **전직 = 원소 + 역할 둘 다 변경 가능** (단순 원소 변경이 아닌 전체 직업 재정의).
- 2026-04-29: **전투 속도 B+D 조합** — 절대값 상향(시네마틱 회복) + UI 노출(유저 통제). C(0.5x 추가) 옵션은 미사용.
- 2026-04-29: **튜토리얼 원소 선택 폐기 → 신성 시작 + 전직** (다음 세션 작업).
- 2026-04-29: **fire 원소 보너스 비대칭 유지** — 결정 3 (원소 보너스는 그대로) 로 미수정.

## 🔧 수정된 파일 목록

### 커밋 `dfea28d` (NPC 화면 진입 + AoE dim + NRG)
- `js/51_game_town.js` — tavern/library/castle NPC choices 의 wrapper 메서드 사용
- `js/52_game_tavern.js` — `showTavernHeroEntry` wrapper 신규 (후속 삭제됨)
- `js/53_game_deck.js` — `showCodexView` wrapper 신규
- `js/54_game_castle.js` — `showCastleQuestEntry` wrapper 신규
- `js/60_turnbattle_v2.js` — `clearTargetHighlight` 2 지점 추가, 1419 NRG 이중방어
- `design/changelog.md` + `design/current-focus.md` 갱신

### 커밋 `9de8017` (영웅 단일화 + 속도 B+D + 인벤토리)
- `index.html` — 영웅/용병 탭 버튼 폐기 + bv2-hud-top 속도 버튼 그룹
- `css/41_battle_v2.css` — `.bsm-speed-group/-btn/.is-active` + `bv2-hud-top` justify-content 변경
- `js/51_game_town.js` — tavern NPC choice "영웅 영입하기" 라인 삭제
- `js/52_game_tavern.js` — `_tavTab/showTavernHero/showTavernHeroEntry/genHeroRecruitCards` 4종 삭제. `showTavernUnit` 단순화.
- `js/60_turnbattle_v2.js` — `v2.setSpeed` HANDLER 추가
- `js/config_battle.js` — TIMING 4개 절대값 상향
- `design/units_skills_inventory_2026-04-29.txt` (신규)
- `design/changelog.md` + `design/current-focus.md` 갱신

### 운영 트리 (별도 push 안 됨)
- `c:/work/.claude/rules/08-garbage-lessons.md` — 6번째 교훈 (HTML ↔ JS 경계)

## 📂 관련 참조 문서

- `design/current-focus.md` — 다음 세션 시작 시 자동 로드 (최신 상태)
- `design/changelog.md` — 결정 히스토리 (2026-04-29 항목 2개)
- `design/units_skills_inventory_2026-04-29.txt` — 영웅/유닛/스킬 종합 + 이펙트 워크플로
- `design/lore-bible.md` — 세계관 v2 (제1세대 + 신좌×점유자)
- `.claude/rules/04-balance.md` — 밸런스 Source of Truth
- `.claude/rules/08-garbage-lessons.md` — 회귀 패턴 교훈집 (6건)
- `js/11_data_heroes.js` — 영웅 매트릭스 (36 조합 + createHero)
- `js/52_game_tavern.js` — 선술집 (용병 전용으로 단순화)

## 🧠 다음 세션 진입 시 확인 사항

1. **회귀 테스트 1회** — `cd c:/work/game && node tools/test_run.js`. 12/12 PASS 보장.
2. **직업 트리 시스템 후보 직업 N개** 사용자에게 확인 — 마검사 외에 성기사/암살자/정령사 등 어떤 게 있어야 할지.
3. **무기 종류 정리** — 검/지팡이/단검/활/도끼/방패 등 어떤 카테고리?
4. **6원소 스펠 균등 분배** — 직업 트리 가능하게 하려면 원소별 스펠 N장씩 (예: 6×3 = 18장) 추가 필요.

---

<!-- Claude Code 지시사항: 이 문서는 이전 세션의 핸드오프입니다.
이 내용을 읽고 사용자에게 다음과 같이 응답하세요:

"🔄 핸드오프 복구 완료.

마지막 작업: 발견 버그 4건 fix + 자율감사 3종 + 영웅 시스템 단일화 + 전투 속도 B+D + 직업 트리 시스템 결정 — 2 커밋
다음 작업: 직업 트리 시스템 Step 1 — 기획서 작성 (마검사 등 N개 직업 + 해금 3축 + 효과)
해결 안 된 것: 직접 플레이 검증 / balance P0 5건 / lore Critical 5건 / 무기·스펠 레벨 선행 시스템 부재 / bonusTrigger v2 이식

이어서 'Step 1 직업 트리 기획서 작성' 부터 진행할까요? 후보 직업들 어떤 게 있어야 할지 사용자 의견부터 받겠습니다."

응답 후 사용자의 지시를 기다리세요. -->
